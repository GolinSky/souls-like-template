# Merge this table into the parent/project .codex/config.toml.
# Keep the primary/orchestrator model separate from these subagent defaults.

[agents]
enabled = true
max_concurrent_threads_per_session = 4
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "high"
interrupt_message = true
